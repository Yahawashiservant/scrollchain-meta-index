{
  "name": "scrollchain-meta-index",
  "version": "1.0.0",
  "description": "Sovereign kernel minting and dashboard deployment for ScrollChainOS",
  "private": true,
  "workspaces": [
    "cockpit",
    "agents/*",
    "kernels/*",
    "rag/*",
    "functions/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace cockpit\" \"npm run dev --workspace functions\"",
    "build": "npm run build --workspace cockpit",
    "start": "npm run start --workspace cockpit"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "author": "Keith D. Whitfield",
  "license": "MIT"
}
